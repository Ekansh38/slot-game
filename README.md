# slot-game

A terminal slot machine game with fake money.

## Features

- Classic slot machine with 3 reels
- Adjustable bets, no cap
- Persistent balance saved between sessions
- Start with $1000

## Run

```bash
pip install rich
python main.py
```

## Controls

- `Up / Down` to adjust bet
- `Enter` to spin
- `Q` to quit
